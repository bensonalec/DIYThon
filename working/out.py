from parser import memoize, memoize_left_rec, Parser
import tokenize
class file0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class interactive0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class eval0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class func_type0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class fstring0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class type_expressions0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class type_expressions1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class type_expressions2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class type_expressions3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class type_expressions4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class type_expressions5:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class type_expressions6:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class statements0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class statement0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class statement1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class statement_newline0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class statement_newline1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class statement_newline2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class statement_newline3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmts0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmts1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmt1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmt2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmt3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmt4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmt5:
	def __init__(self,  *rest):
		pass
	def translate(self):
		return f"{'pass'}"
class simple_stmt6:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmt7:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmt8:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmt9:
	def __init__(self,  *rest):
		pass
	def translate(self):
		return f"{'break'}"
class simple_stmt10:
	def __init__(self,  *rest):
		pass
	def translate(self):
		return f"{'continue'}"
class simple_stmt11:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class simple_stmt12:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compound_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compound_stmt1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compound_stmt2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compound_stmt3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compound_stmt4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compound_stmt5:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compound_stmt6:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compound_stmt7:
	def __init__(self, _b, *rest):
		self._b = _b
		pass
	def translate(self):
		return f"{self._b.translate() if type(self._b) != str else self._b}"
class assignment0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class assignment1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class assignment2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class assignment3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign5:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign6:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign7:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign8:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign9:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign10:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign11:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class augassign12:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class global_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class nonlocal_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class yield_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class assert_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class del_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class import_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class import_stmt1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class import_name0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class import_from0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class import_from1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class import_from_targets0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class import_from_targets1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class import_from_targets2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class import_from_as_names0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class import_from_as_name0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class dotted_as_names0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class dotted_as_name0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class dotted_name0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class dotted_name1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class if_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class if_stmt1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class elif_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class elif_stmt1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class else_block0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class while_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class for_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class for_stmt1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class with_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class with_stmt1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class with_stmt2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class with_stmt3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class with_item0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class with_item1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class try_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class try_stmt1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class except_block0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class except_block1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class finally_block0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class match_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class subject_expr0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class subject_expr1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class case_block0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class guard0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class patterns0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class patterns1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class pattern1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class as_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class or_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class closed_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class closed_pattern1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class closed_pattern2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class closed_pattern3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class closed_pattern4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class closed_pattern5:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class closed_pattern6:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class closed_pattern7:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_pattern1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_pattern2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_pattern3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_pattern4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_pattern5:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_expr0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_expr1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_expr2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_expr3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_expr4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class literal_expr5:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class complex_number0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class complex_number1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class signed_number0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class signed_number1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class signed_real_number0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class signed_real_number1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class real_number0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class imaginary_number0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class capture_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class pattern_capture_target0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class wildcard_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class value_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class attr0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class name_or_attr0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class name_or_attr1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class group_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class sequence_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class sequence_pattern1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class open_sequence_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class maybe_sequence_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class maybe_star_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class maybe_star_pattern1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_pattern1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class mapping_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class mapping_pattern1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class mapping_pattern2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class mapping_pattern3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class items_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class key_value_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class double_star_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class class_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class class_pattern1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class class_pattern2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class class_pattern3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class positional_patterns0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class keyword_patterns0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class keyword_pattern0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class return_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class raise_stmt0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class raise_stmt1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class function_def0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class function_def1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class function_def_raw0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class function_def_raw1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class func_type_comment0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class func_type_comment1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class params0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class parameters0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class parameters1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class parameters2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class parameters3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class parameters4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class slash_no_default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class slash_no_default1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class slash_with_default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class slash_with_default1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_etc0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_etc1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_etc2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class kwds0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class param_no_default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class param_no_default1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class param_with_default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class param_with_default1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class param_maybe_default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class param_maybe_default1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class param0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class annotation0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class decorators0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class class_def0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class class_def1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class class_def_raw0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class block0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class block1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_expressions0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_expressions1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_expressions2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_expression0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_expression1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_named_expressions0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_named_expression0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_named_expression1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class assignment_expression0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class named_expression0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class named_expression1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class annotated_rhs0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class annotated_rhs1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class expressions0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class expressions1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class expressions2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class expression0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class expression1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class expression2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambdef0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_params0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_parameters0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_parameters1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_parameters2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_parameters3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_parameters4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_slash_no_default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_slash_no_default1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_slash_with_default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_slash_with_default1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_star_etc0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_star_etc1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_star_etc2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_kwds0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_param_no_default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_param_no_default1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_param_with_default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_param_with_default1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_param_maybe_default0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_param_maybe_default1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lambda_param0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class disjunction0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class disjunction1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class conjunction0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class conjunction1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class inversion0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class inversion1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class comparison0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class comparison1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compare_op_bitwise_or_pair0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compare_op_bitwise_or_pair1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compare_op_bitwise_or_pair2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compare_op_bitwise_or_pair3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compare_op_bitwise_or_pair4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compare_op_bitwise_or_pair5:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compare_op_bitwise_or_pair6:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compare_op_bitwise_or_pair7:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compare_op_bitwise_or_pair8:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class compare_op_bitwise_or_pair9:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class eq_bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class noteq_bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lte_bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class lt_bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class gte_bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class gt_bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class notin_bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class in_bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class isnot_bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class is_bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class bitwise_or0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class bitwise_or1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class bitwise_xor0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class bitwise_xor1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class bitwise_and0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class bitwise_and1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class shift_expr0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class shift_expr1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class shift_expr2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class sum0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class sum1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class sum2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class term0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class term1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class term2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class term3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class term4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class term5:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class factor0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class factor1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class factor2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class factor3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class power0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class power1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class await_primary0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class await_primary1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class primary0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class primary1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class primary2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class primary3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class primary4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class slices0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class slices1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class slice0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class slice1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class atom0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class atom1:
	def __init__(self,  *rest):
		pass
	def translate(self):
		return f"{'True'}"
class atom2:
	def __init__(self,  *rest):
		pass
	def translate(self):
		return f"{'False'}"
class atom3:
	def __init__(self,  *rest):
		pass
	def translate(self):
		return f"{'None'}"
class atom4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class atom5:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class atom6:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class atom7:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class atom8:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class atom9:
	def __init__(self,  *rest):
		pass
	def translate(self):
		return f"{'...'}"
class strings0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class list0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class listcomp0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class tuple0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class group0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class genexp0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class set0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class setcomp0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class dict0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class dict1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class dictcomp0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class double_starred_kvpairs0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class double_starred_kvpair0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class double_starred_kvpair1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class kvpair0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class for_if_clauses0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class for_if_clause0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class for_if_clause1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class yield_expr0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class yield_expr1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class arguments0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class args0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class args1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class kwargs0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class kwargs1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class kwargs2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class starred_expression0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class kwarg_or_starred0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class kwarg_or_starred1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class kwarg_or_double_starred0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class kwarg_or_double_starred1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_targets0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_targets1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_targets_list_seq0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_targets_tuple_seq0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_targets_tuple_seq1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_target0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_target1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class target_with_star_atom0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class target_with_star_atom1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class target_with_star_atom2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_atom0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_atom1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_atom2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class star_atom3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class single_target0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class single_target1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class single_target2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class single_subscript_attribute_target0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class single_subscript_attribute_target1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class del_targets0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class del_target0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class del_target1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class del_target2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class del_t_atom0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class del_t_atom1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class del_t_atom2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class del_t_atom3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class t_primary0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class t_primary1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class t_primary2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class t_primary3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class t_primary4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class t_lookahead0:
	def __init__(self,  *rest):
		pass
	def translate(self):
		return f"{'('}"
class t_lookahead1:
	def __init__(self,  *rest):
		pass
	def translate(self):
		return f"{'['}"
class t_lookahead2:
	def __init__(self,  *rest):
		pass
	def translate(self):
		return f"{'.'}"
class invalid_double_starred_kvpairs0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class invalid_double_starred_kvpairs1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class invalid_double_starred_kvpairs2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class invalid_kvpair0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class invalid_kvpair1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class invalid_kvpair2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_0:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_1:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_2:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_3:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_4:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_5:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_6:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_7:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_8:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_9:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_10:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_11:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_12:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_13:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_14:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_14:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_15:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_16:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_17:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_17:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_18:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_19:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_19:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_20:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_21:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_22:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_23:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_24:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_25:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_25:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_26:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_26:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_27:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_27:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_28:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_29:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_30:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_31:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_32:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_33:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_34:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_35:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_36:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_37:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_38:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_39:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_40:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_41:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_42:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_43:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_44:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_45:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_46:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_47:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_48:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_49:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_50:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_51:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_52:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_52:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_52:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_53:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_54:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_55:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_56:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_57:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_58:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_58:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_59:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_59:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_60:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_60:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_60:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_61:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_61:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_61:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_62:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_63:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_64:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_65:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_66:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_66:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_67:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_68:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_69:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_70:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_71:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_72:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_73:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_74:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_75:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_76:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_77:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_78:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_79:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_80:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_81:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_82:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_83:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_84:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_85:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_86:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_87:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_88:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_89:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_90:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_91:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_92:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_93:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_94:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_95:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_96:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_97:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_98:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_99:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_100:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_101:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_102:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_103:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_104:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_105:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_106:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_107:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_108:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_109:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_110:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_111:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_112:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_113:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_114:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_114:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_114:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_115:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_115:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_116:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_116:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_116:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_116:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_117:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_118:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_119:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_120:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_120:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_121:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_121:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_122:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_123:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_124:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_125:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_126:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_127:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_128:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_129:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_130:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_130:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_131:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_131:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_132:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_132:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_133:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_133:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_134:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_135:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_136:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_137:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_138:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_139:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_140:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_141:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_142:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_143:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_144:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_145:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_146:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_147:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_148:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_149:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_150:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_151:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_152:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_153:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_154:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_155:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_156:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_157:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_158:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_159:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_160:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_161:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_161:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class synthetic_rule_162:
	def __init__(self, _a, *rest):
		self._a = _a
		pass
	def translate(self):
		return f"{self._a.translate() if type(self._a) != str else self._a}"
class ToyParser(Parser):
	@memoize
	def file(self):
		pos = self.mark()
		if (True and
		 ((n0 := self.synthetic_rule_0()) or True) is not None and
		 (n1 := self.expect('ENDMARKER')) is not None and
		   True):
			return file0(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def interactive(self):
		pos = self.mark()
		if (True and
		 (n0 := self.statement_newline()) is not None and
		   True):
			return interactive0(n0)
		self.reset(pos)
		return None
	@memoize
	def eval(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expressions()) is not None and
		 (n1 := self.loop(False, self.expect, 'NEWLINE')) is not None and
		 (n2 := self.expect('ENDMARKER')) is not None and
		   True):
			return eval0(n0, n1, n2)
		self.reset(pos)
		return None
	@memoize
	def func_type(self):
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 ((n1 := self.synthetic_rule_1()) or True) is not None and
		 self.expect(')') is not None and
		 self.expect('->') is not None and
		 (n4 := self.expression()) is not None and
		 (n5 := self.loop(False, self.expect, 'NEWLINE')) is not None and
		 (n6 := self.expect('ENDMARKER')) is not None and
		   True):
			return func_type0(n1, n4, n5, n6)
		self.reset(pos)
		return None
	@memoize
	def fstring(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_expressions()) is not None and
		   True):
			return fstring0(n0)
		self.reset(pos)
		return None
	@memoize
	def type_expressions(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_3()) is not None and
		 self.expect(',') is not None and
		 self.expect('*') is not None and
		 (n3 := self.expression()) is not None and
		 self.expect(',') is not None and
		 self.expect('**') is not None and
		 (n6 := self.expression()) is not None and
		   True):
			return type_expressions0(n0, n3, n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n7 := self.synthetic_rule_5()) is not None and
		 self.expect(',') is not None and
		 self.expect('*') is not None and
		 (n10 := self.expression()) is not None and
		   True):
			return type_expressions1(n7, n10)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n11 := self.synthetic_rule_7()) is not None and
		 self.expect(',') is not None and
		 self.expect('**') is not None and
		 (n14 := self.expression()) is not None and
		   True):
			return type_expressions2(n11, n14)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 (n16 := self.expression()) is not None and
		 self.expect(',') is not None and
		 self.expect('**') is not None and
		 (n19 := self.expression()) is not None and
		   True):
			return type_expressions3(n16, n19)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 (n21 := self.expression()) is not None and
		   True):
			return type_expressions4(n21)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('**') is not None and
		 (n23 := self.expression()) is not None and
		   True):
			return type_expressions5(n23)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n24 := self.synthetic_rule_9()) is not None and
		   True):
			return type_expressions6(n24)
		self.reset(pos)
		return None
	@memoize
	def statements(self):
		pos = self.mark()
		if (True and
		 (n0 := self.loop(True, self.statement, )) is not None and
		   True):
			return statements0(n0)
		self.reset(pos)
		return None
	@memoize
	def statement(self):
		pos = self.mark()
		if (True and
		 (n0 := self.compound_stmt()) is not None and
		   True):
			return statement0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.simple_stmts()) is not None and
		   True):
			return statement1(n1)
		self.reset(pos)
		return None
	@memoize
	def statement_newline(self):
		pos = self.mark()
		if (True and
		 (n0 := self.compound_stmt()) is not None and
		 (n1 := self.expect('NEWLINE')) is not None and
		   True):
			return statement_newline0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.simple_stmts()) is not None and
		   True):
			return statement_newline1(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.expect('NEWLINE')) is not None and
		   True):
			return statement_newline2(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.expect('ENDMARKER')) is not None and
		   True):
			return statement_newline3(n4)
		self.reset(pos)
		return None
	@memoize
	def simple_stmts(self):
		pos = self.mark()
		if (True and
		 (n0 := self.simple_stmt()) is not None and
		 self.negative_lookahead(self.expect, ';') is not None and
		 (n2 := self.expect('NEWLINE')) is not None and
		   True):
			return simple_stmts0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.synthetic_rule_11()) is not None and
		 ((n4 := self.synthetic_rule_12()) or True) is not None and
		 (n5 := self.expect('NEWLINE')) is not None and
		   True):
			return simple_stmts1(n3, n4, n5)
		self.reset(pos)
		return None
	@memoize
	def simple_stmt(self):
		pos = self.mark()
		if (True and
		 (n0 := self.assignment()) is not None and
		   True):
			return simple_stmt0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.star_expressions()) is not None and
		   True):
			return simple_stmt1(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.return_stmt()) is not None and
		   True):
			return simple_stmt2(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.import_stmt()) is not None and
		   True):
			return simple_stmt3(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.raise_stmt()) is not None and
		   True):
			return simple_stmt4(n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('pass') is not None and
		   True):
			return simple_stmt5()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.del_stmt()) is not None and
		   True):
			return simple_stmt6(n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n7 := self.yield_stmt()) is not None and
		   True):
			return simple_stmt7(n7)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n8 := self.assert_stmt()) is not None and
		   True):
			return simple_stmt8(n8)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('break') is not None and
		   True):
			return simple_stmt9()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('continue') is not None and
		   True):
			return simple_stmt10()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n11 := self.global_stmt()) is not None and
		   True):
			return simple_stmt11(n11)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n12 := self.nonlocal_stmt()) is not None and
		   True):
			return simple_stmt12(n12)
		self.reset(pos)
		return None
	@memoize
	def compound_stmt(self):
		pos = self.mark()
		if (True and
		 (n0 := self.function_def()) is not None and
		   True):
			return compound_stmt0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.if_stmt()) is not None and
		   True):
			return compound_stmt1(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.class_def()) is not None and
		   True):
			return compound_stmt2(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.with_stmt()) is not None and
		   True):
			return compound_stmt3(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.for_stmt()) is not None and
		   True):
			return compound_stmt4(n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n5 := self.try_stmt()) is not None and
		   True):
			return compound_stmt5(n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.while_stmt()) is not None and
		   True):
			return compound_stmt6(n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n7 := self.match_stmt()) is not None and
		   True):
			return compound_stmt7(n7)
		self.reset(pos)
		return None
	@memoize
	def assignment(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		 self.expect(':') is not None and
		 (n2 := self.expression()) is not None and
		 ((n3 := self.synthetic_rule_13()) or True) is not None and
		   True):
			return assignment0(n0, n2, n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.synthetic_rule_14()) is not None and
		 self.expect(':') is not None and
		 (n6 := self.expression()) is not None and
		 ((n7 := self.synthetic_rule_15()) or True) is not None and
		   True):
			return assignment1(n4, n6, n7)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n8 := self.loop(True, self.synthetic_rule_16, )) is not None and
		 (n9 := self.synthetic_rule_17()) is not None and
		 self.negative_lookahead(self.expect, '=') is not None and
		 ((n11 := self.synthetic_rule_18()) or True) is not None and
		   True):
			return assignment2(n8, n9, n11)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n12 := self.single_target()) is not None and
		 (n13 := self.augassign()) is not None and
		 True is not None and
		 (n15 := self.synthetic_rule_19()) is not None and
		   True):
			return assignment3(n12, n13, n15)
		self.reset(pos)
		return None
	@memoize
	def augassign(self):
		pos = self.mark()
		if (True and
		 self.expect('+=') is not None and
		   True):
			return augassign0()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('-=') is not None and
		   True):
			return augassign1()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('*=') is not None and
		   True):
			return augassign2()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('@=') is not None and
		   True):
			return augassign3()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('/=') is not None and
		   True):
			return augassign4()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('%=') is not None and
		   True):
			return augassign5()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('&=') is not None and
		   True):
			return augassign6()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('|=') is not None and
		   True):
			return augassign7()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('^=') is not None and
		   True):
			return augassign8()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('<<=') is not None and
		   True):
			return augassign9()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('>>=') is not None and
		   True):
			return augassign10()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('**=') is not None and
		   True):
			return augassign11()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('//=') is not None and
		   True):
			return augassign12()
		self.reset(pos)
		return None
	@memoize
	def global_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('global') is not None and
		 (n1 := self.synthetic_rule_21()) is not None and
		   True):
			return global_stmt0(n1)
		self.reset(pos)
		return None
	@memoize
	def nonlocal_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('nonlocal') is not None and
		 (n1 := self.synthetic_rule_23()) is not None and
		   True):
			return nonlocal_stmt0(n1)
		self.reset(pos)
		return None
	@memoize
	def yield_stmt(self):
		pos = self.mark()
		if (True and
		 (n0 := self.yield_expr()) is not None and
		   True):
			return yield_stmt0(n0)
		self.reset(pos)
		return None
	@memoize
	def assert_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('assert') is not None and
		 (n1 := self.expression()) is not None and
		 ((n2 := self.synthetic_rule_24()) or True) is not None and
		   True):
			return assert_stmt0(n1, n2)
		self.reset(pos)
		return None
	@memoize
	def del_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('del') is not None and
		 (n1 := self.del_targets()) is not None and
		 self.positive_lookahead(self.synthetic_rule_25, ) is not None and
		   True):
			return del_stmt0(n1)
		self.reset(pos)
		return None
	@memoize
	def import_stmt(self):
		pos = self.mark()
		if (True and
		 (n0 := self.import_name()) is not None and
		   True):
			return import_stmt0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.import_from()) is not None and
		   True):
			return import_stmt1(n1)
		self.reset(pos)
		return None
	@memoize
	def import_name(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('import') is not None and
		 (n1 := self.dotted_as_names()) is not None and
		   True):
			return import_name0(n1)
		self.reset(pos)
		return None
	@memoize
	def import_from(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('from') is not None and
		 (n1 := self.loop(False, self.synthetic_rule_26, )) is not None and
		 (n2 := self.dotted_name()) is not None and
		 self.expect_keyword('import') is not None and
		 (n4 := self.import_from_targets()) is not None and
		   True):
			return import_from0(n1, n2, n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('from') is not None and
		 (n6 := self.loop(True, self.synthetic_rule_27, )) is not None and
		 self.expect_keyword('import') is not None and
		 (n8 := self.import_from_targets()) is not None and
		   True):
			return import_from1(n6, n8)
		self.reset(pos)
		return None
	@memoize
	def import_from_targets(self):
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 (n1 := self.import_from_as_names()) is not None and
		 ((n2 := self.synthetic_rule_28()) or True) is not None and
		 self.expect(')') is not None and
		   True):
			return import_from_targets0(n1, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.import_from_as_names()) is not None and
		 self.negative_lookahead(self.expect, ',') is not None and
		   True):
			return import_from_targets1(n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		   True):
			return import_from_targets2()
		self.reset(pos)
		return None
	@memoize
	def import_from_as_names(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_30()) is not None and
		   True):
			return import_from_as_names0(n0)
		self.reset(pos)
		return None
	@memoize
	def import_from_as_name(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		 ((n1 := self.synthetic_rule_31()) or True) is not None and
		   True):
			return import_from_as_name0(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def dotted_as_names(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_33()) is not None and
		   True):
			return dotted_as_names0(n0)
		self.reset(pos)
		return None
	@memoize
	def dotted_as_name(self):
		pos = self.mark()
		if (True and
		 (n0 := self.dotted_name()) is not None and
		 ((n1 := self.synthetic_rule_34()) or True) is not None and
		   True):
			return dotted_as_name0(n0, n1)
		self.reset(pos)
		return None
	@memoize_left_rec
	def dotted_name(self):
		pos = self.mark()
		if (True and
		 (n0 := self.dotted_name()) is not None and
		 self.expect('.') is not None and
		 (n2 := self.expect('NAME')) is not None and
		   True):
			return dotted_name0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.expect('NAME')) is not None and
		   True):
			return dotted_name1(n3)
		self.reset(pos)
		return None
	@memoize
	def if_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('if') is not None and
		 (n1 := self.named_expression()) is not None and
		 self.expect(':') is not None and
		 (n3 := self.block()) is not None and
		 (n4 := self.elif_stmt()) is not None and
		   True):
			return if_stmt0(n1, n3, n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('if') is not None and
		 (n6 := self.named_expression()) is not None and
		 self.expect(':') is not None and
		 (n8 := self.block()) is not None and
		 ((n9 := self.synthetic_rule_35()) or True) is not None and
		   True):
			return if_stmt1(n6, n8, n9)
		self.reset(pos)
		return None
	@memoize
	def elif_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('elif') is not None and
		 (n1 := self.named_expression()) is not None and
		 self.expect(':') is not None and
		 (n3 := self.block()) is not None and
		 (n4 := self.elif_stmt()) is not None and
		   True):
			return elif_stmt0(n1, n3, n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('elif') is not None and
		 (n6 := self.named_expression()) is not None and
		 self.expect(':') is not None and
		 (n8 := self.block()) is not None and
		 ((n9 := self.synthetic_rule_36()) or True) is not None and
		   True):
			return elif_stmt1(n6, n8, n9)
		self.reset(pos)
		return None
	@memoize
	def else_block(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('else') is not None and
		 self.expect(':') is not None and
		 (n2 := self.block()) is not None and
		   True):
			return else_block0(n2)
		self.reset(pos)
		return None
	@memoize
	def while_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('while') is not None and
		 (n1 := self.named_expression()) is not None and
		 self.expect(':') is not None and
		 (n3 := self.block()) is not None and
		 ((n4 := self.synthetic_rule_37()) or True) is not None and
		   True):
			return while_stmt0(n1, n3, n4)
		self.reset(pos)
		return None
	@memoize
	def for_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('for') is not None and
		 (n1 := self.star_targets()) is not None and
		 self.expect_keyword('in') is not None and
		 True is not None and
		 (n4 := self.star_expressions()) is not None and
		 self.expect(':') is not None and
		 ((n6 := self.synthetic_rule_38()) or True) is not None and
		 (n7 := self.block()) is not None and
		 ((n8 := self.synthetic_rule_39()) or True) is not None and
		   True):
			return for_stmt0(n1, n4, n6, n7, n8)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n9 := self.expect('ASYNC')) is not None and
		 self.expect_keyword('for') is not None and
		 (n11 := self.star_targets()) is not None and
		 self.expect_keyword('in') is not None and
		 True is not None and
		 (n14 := self.star_expressions()) is not None and
		 self.expect(':') is not None and
		 ((n16 := self.synthetic_rule_40()) or True) is not None and
		 (n17 := self.block()) is not None and
		 ((n18 := self.synthetic_rule_41()) or True) is not None and
		   True):
			return for_stmt1(n9, n11, n14, n16, n17, n18)
		self.reset(pos)
		return None
	@memoize
	def with_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('with') is not None and
		 self.expect('(') is not None and
		 (n2 := self.synthetic_rule_43()) is not None and
		 (self.expect(',') or True) is not None and
		 self.expect(')') is not None and
		 self.expect(':') is not None and
		 (n6 := self.block()) is not None and
		   True):
			return with_stmt0(n2, n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('with') is not None and
		 (n8 := self.synthetic_rule_45()) is not None and
		 self.expect(':') is not None and
		 ((n10 := self.synthetic_rule_46()) or True) is not None and
		 (n11 := self.block()) is not None and
		   True):
			return with_stmt1(n8, n10, n11)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n12 := self.expect('ASYNC')) is not None and
		 self.expect_keyword('with') is not None and
		 self.expect('(') is not None and
		 (n15 := self.synthetic_rule_48()) is not None and
		 (self.expect(',') or True) is not None and
		 self.expect(')') is not None and
		 self.expect(':') is not None and
		 (n19 := self.block()) is not None and
		   True):
			return with_stmt2(n12, n15, n19)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n20 := self.expect('ASYNC')) is not None and
		 self.expect_keyword('with') is not None and
		 (n22 := self.synthetic_rule_50()) is not None and
		 self.expect(':') is not None and
		 ((n24 := self.synthetic_rule_51()) or True) is not None and
		 (n25 := self.block()) is not None and
		   True):
			return with_stmt3(n20, n22, n24, n25)
		self.reset(pos)
		return None
	@memoize
	def with_item(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		 self.expect_keyword('as') is not None and
		 (n2 := self.star_target()) is not None and
		 self.positive_lookahead(self.synthetic_rule_52, ) is not None and
		   True):
			return with_item0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.expression()) is not None and
		   True):
			return with_item1(n4)
		self.reset(pos)
		return None
	@memoize
	def try_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('try') is not None and
		 self.expect(':') is not None and
		 (n2 := self.block()) is not None and
		 (n3 := self.finally_block()) is not None and
		   True):
			return try_stmt0(n2, n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('try') is not None and
		 self.expect(':') is not None and
		 (n6 := self.block()) is not None and
		 (n7 := self.loop(True, self.except_block, )) is not None and
		 ((n8 := self.synthetic_rule_53()) or True) is not None and
		 ((n9 := self.synthetic_rule_54()) or True) is not None and
		   True):
			return try_stmt1(n6, n7, n8, n9)
		self.reset(pos)
		return None
	@memoize
	def except_block(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('except') is not None and
		 (n1 := self.expression()) is not None and
		 ((n2 := self.synthetic_rule_55()) or True) is not None and
		 self.expect(':') is not None and
		 (n4 := self.block()) is not None and
		   True):
			return except_block0(n1, n2, n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('except') is not None and
		 self.expect(':') is not None and
		 (n7 := self.block()) is not None and
		   True):
			return except_block1(n7)
		self.reset(pos)
		return None
	@memoize
	def finally_block(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('finally') is not None and
		 self.expect(':') is not None and
		 (n2 := self.block()) is not None and
		   True):
			return finally_block0(n2)
		self.reset(pos)
		return None
	@memoize
	def match_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect("match") is not None and
		 (n1 := self.subject_expr()) is not None and
		 self.expect(':') is not None and
		 (n3 := self.expect('NEWLINE')) is not None and
		 (n4 := self.expect('INDENT')) is not None and
		 (n5 := self.loop(True, self.case_block, )) is not None and
		 (n6 := self.expect('DEDENT')) is not None and
		   True):
			return match_stmt0(n1, n3, n4, n5, n6)
		self.reset(pos)
		return None
	@memoize
	def subject_expr(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_named_expression()) is not None and
		 self.expect(',') is not None and
		 ((n2 := self.star_named_expressions()) or True) is not None and
		   True):
			return subject_expr0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.named_expression()) is not None and
		   True):
			return subject_expr1(n3)
		self.reset(pos)
		return None
	@memoize
	def case_block(self):
		pos = self.mark()
		if (True and
		 self.expect("case") is not None and
		 (n1 := self.patterns()) is not None and
		 ((n2 := self.guard()) or True) is not None and
		 self.expect(':') is not None and
		 (n4 := self.block()) is not None and
		   True):
			return case_block0(n1, n2, n4)
		self.reset(pos)
		return None
	@memoize
	def guard(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('if') is not None and
		 (n1 := self.named_expression()) is not None and
		   True):
			return guard0(n1)
		self.reset(pos)
		return None
	@memoize
	def patterns(self):
		pos = self.mark()
		if (True and
		 (n0 := self.open_sequence_pattern()) is not None and
		   True):
			return patterns0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.pattern()) is not None and
		   True):
			return patterns1(n1)
		self.reset(pos)
		return None
	@memoize
	def pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.as_pattern()) is not None and
		   True):
			return pattern0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.or_pattern()) is not None and
		   True):
			return pattern1(n1)
		self.reset(pos)
		return None
	@memoize
	def as_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.or_pattern()) is not None and
		 self.expect_keyword('as') is not None and
		 (n2 := self.pattern_capture_target()) is not None and
		   True):
			return as_pattern0(n0, n2)
		self.reset(pos)
		return None
	@memoize
	def or_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_57()) is not None and
		   True):
			return or_pattern0(n0)
		self.reset(pos)
		return None
	@memoize
	def closed_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.literal_pattern()) is not None and
		   True):
			return closed_pattern0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.capture_pattern()) is not None and
		   True):
			return closed_pattern1(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.wildcard_pattern()) is not None and
		   True):
			return closed_pattern2(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.value_pattern()) is not None and
		   True):
			return closed_pattern3(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.group_pattern()) is not None and
		   True):
			return closed_pattern4(n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n5 := self.sequence_pattern()) is not None and
		   True):
			return closed_pattern5(n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.mapping_pattern()) is not None and
		   True):
			return closed_pattern6(n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n7 := self.class_pattern()) is not None and
		   True):
			return closed_pattern7(n7)
		self.reset(pos)
		return None
	@memoize
	def literal_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.signed_number()) is not None and
		 self.negative_lookahead(self.synthetic_rule_58, ) is not None and
		   True):
			return literal_pattern0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.complex_number()) is not None and
		   True):
			return literal_pattern1(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.strings()) is not None and
		   True):
			return literal_pattern2(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('None') is not None and
		   True):
			return literal_pattern3()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('True') is not None and
		   True):
			return literal_pattern4()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('False') is not None and
		   True):
			return literal_pattern5()
		self.reset(pos)
		return None
	@memoize
	def literal_expr(self):
		pos = self.mark()
		if (True and
		 (n0 := self.signed_number()) is not None and
		 self.negative_lookahead(self.synthetic_rule_59, ) is not None and
		   True):
			return literal_expr0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.complex_number()) is not None and
		   True):
			return literal_expr1(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.strings()) is not None and
		   True):
			return literal_expr2(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('None') is not None and
		   True):
			return literal_expr3()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('True') is not None and
		   True):
			return literal_expr4()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('False') is not None and
		   True):
			return literal_expr5()
		self.reset(pos)
		return None
	@memoize
	def complex_number(self):
		pos = self.mark()
		if (True and
		 (n0 := self.signed_real_number()) is not None and
		 self.expect('+') is not None and
		 (n2 := self.imaginary_number()) is not None and
		   True):
			return complex_number0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.signed_real_number()) is not None and
		 self.expect('-') is not None and
		 (n5 := self.imaginary_number()) is not None and
		   True):
			return complex_number1(n3, n5)
		self.reset(pos)
		return None
	@memoize
	def signed_number(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NUMBER')) is not None and
		   True):
			return signed_number0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('-') is not None and
		 (n2 := self.expect('NUMBER')) is not None and
		   True):
			return signed_number1(n2)
		self.reset(pos)
		return None
	@memoize
	def signed_real_number(self):
		pos = self.mark()
		if (True and
		 (n0 := self.real_number()) is not None and
		   True):
			return signed_real_number0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('-') is not None and
		 (n2 := self.real_number()) is not None and
		   True):
			return signed_real_number1(n2)
		self.reset(pos)
		return None
	@memoize
	def real_number(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NUMBER')) is not None and
		   True):
			return real_number0(n0)
		self.reset(pos)
		return None
	@memoize
	def imaginary_number(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NUMBER')) is not None and
		   True):
			return imaginary_number0(n0)
		self.reset(pos)
		return None
	@memoize
	def capture_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.pattern_capture_target()) is not None and
		   True):
			return capture_pattern0(n0)
		self.reset(pos)
		return None
	@memoize
	def pattern_capture_target(self):
		pos = self.mark()
		if (True and
		 self.negative_lookahead(self.expect, "_") is not None and
		 (n1 := self.expect('NAME')) is not None and
		 self.negative_lookahead(self.synthetic_rule_60, ) is not None and
		   True):
			return pattern_capture_target0(n1)
		self.reset(pos)
		return None
	@memoize
	def wildcard_pattern(self):
		pos = self.mark()
		if (True and
		 self.expect("_") is not None and
		   True):
			return wildcard_pattern0()
		self.reset(pos)
		return None
	@memoize
	def value_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.attr()) is not None and
		 self.negative_lookahead(self.synthetic_rule_61, ) is not None and
		   True):
			return value_pattern0(n0)
		self.reset(pos)
		return None
	@memoize_left_rec
	def attr(self):
		pos = self.mark()
		if (True and
		 (n0 := self.name_or_attr()) is not None and
		 self.expect('.') is not None and
		 (n2 := self.expect('NAME')) is not None and
		   True):
			return attr0(n0, n2)
		self.reset(pos)
		return None
	@memoize_left_rec
	def name_or_attr(self):
		pos = self.mark()
		if (True and
		 (n0 := self.attr()) is not None and
		   True):
			return name_or_attr0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.expect('NAME')) is not None and
		   True):
			return name_or_attr1(n1)
		self.reset(pos)
		return None
	@memoize
	def group_pattern(self):
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 (n1 := self.pattern()) is not None and
		 self.expect(')') is not None and
		   True):
			return group_pattern0(n1)
		self.reset(pos)
		return None
	@memoize
	def sequence_pattern(self):
		pos = self.mark()
		if (True and
		 self.expect('[') is not None and
		 ((n1 := self.maybe_sequence_pattern()) or True) is not None and
		 self.expect(']') is not None and
		   True):
			return sequence_pattern0(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 ((n4 := self.open_sequence_pattern()) or True) is not None and
		 self.expect(')') is not None and
		   True):
			return sequence_pattern1(n4)
		self.reset(pos)
		return None
	@memoize
	def open_sequence_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.maybe_star_pattern()) is not None and
		 self.expect(',') is not None and
		 ((n2 := self.maybe_sequence_pattern()) or True) is not None and
		   True):
			return open_sequence_pattern0(n0, n2)
		self.reset(pos)
		return None
	@memoize
	def maybe_sequence_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_63()) is not None and
		 (self.expect(',') or True) is not None and
		   True):
			return maybe_sequence_pattern0(n0)
		self.reset(pos)
		return None
	@memoize
	def maybe_star_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_pattern()) is not None and
		   True):
			return maybe_star_pattern0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.pattern()) is not None and
		   True):
			return maybe_star_pattern1(n1)
		self.reset(pos)
		return None
	@memoize
	def star_pattern(self):
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 (n1 := self.pattern_capture_target()) is not None and
		   True):
			return star_pattern0(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 (n3 := self.wildcard_pattern()) is not None and
		   True):
			return star_pattern1(n3)
		self.reset(pos)
		return None
	@memoize
	def mapping_pattern(self):
		pos = self.mark()
		if (True and
		 self.expect('{') is not None and
		 self.expect('}') is not None and
		   True):
			return mapping_pattern0()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('{') is not None and
		 (n3 := self.double_star_pattern()) is not None and
		 (self.expect(',') or True) is not None and
		 self.expect('}') is not None and
		   True):
			return mapping_pattern1(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('{') is not None and
		 (n7 := self.items_pattern()) is not None and
		 self.expect(',') is not None and
		 (n9 := self.double_star_pattern()) is not None and
		 (self.expect(',') or True) is not None and
		 self.expect('}') is not None and
		   True):
			return mapping_pattern2(n7, n9)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('{') is not None and
		 (n13 := self.items_pattern()) is not None and
		 (self.expect(',') or True) is not None and
		 self.expect('}') is not None and
		   True):
			return mapping_pattern3(n13)
		self.reset(pos)
		return None
	@memoize
	def items_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_65()) is not None and
		   True):
			return items_pattern0(n0)
		self.reset(pos)
		return None
	@memoize
	def key_value_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_66()) is not None and
		 self.expect(':') is not None and
		 (n2 := self.pattern()) is not None and
		   True):
			return key_value_pattern0(n0, n2)
		self.reset(pos)
		return None
	@memoize
	def double_star_pattern(self):
		pos = self.mark()
		if (True and
		 self.expect('**') is not None and
		 (n1 := self.pattern_capture_target()) is not None and
		   True):
			return double_star_pattern0(n1)
		self.reset(pos)
		return None
	@memoize
	def class_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.name_or_attr()) is not None and
		 self.expect('(') is not None and
		 self.expect(')') is not None and
		   True):
			return class_pattern0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.name_or_attr()) is not None and
		 self.expect('(') is not None and
		 (n5 := self.positional_patterns()) is not None and
		 (self.expect(',') or True) is not None and
		 self.expect(')') is not None and
		   True):
			return class_pattern1(n3, n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n8 := self.name_or_attr()) is not None and
		 self.expect('(') is not None and
		 (n10 := self.keyword_patterns()) is not None and
		 (self.expect(',') or True) is not None and
		 self.expect(')') is not None and
		   True):
			return class_pattern2(n8, n10)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n13 := self.name_or_attr()) is not None and
		 self.expect('(') is not None and
		 (n15 := self.positional_patterns()) is not None and
		 self.expect(',') is not None and
		 (n17 := self.keyword_patterns()) is not None and
		 (self.expect(',') or True) is not None and
		 self.expect(')') is not None and
		   True):
			return class_pattern3(n13, n15, n17)
		self.reset(pos)
		return None
	@memoize
	def positional_patterns(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_68()) is not None and
		   True):
			return positional_patterns0(n0)
		self.reset(pos)
		return None
	@memoize
	def keyword_patterns(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_70()) is not None and
		   True):
			return keyword_patterns0(n0)
		self.reset(pos)
		return None
	@memoize
	def keyword_pattern(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		 self.expect('=') is not None and
		 (n2 := self.pattern()) is not None and
		   True):
			return keyword_pattern0(n0, n2)
		self.reset(pos)
		return None
	@memoize
	def return_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('return') is not None and
		 ((n1 := self.synthetic_rule_71()) or True) is not None and
		   True):
			return return_stmt0(n1)
		self.reset(pos)
		return None
	@memoize
	def raise_stmt(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('raise') is not None and
		 (n1 := self.expression()) is not None and
		 ((n2 := self.synthetic_rule_72()) or True) is not None and
		   True):
			return raise_stmt0(n1, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('raise') is not None and
		   True):
			return raise_stmt1()
		self.reset(pos)
		return None
	@memoize
	def function_def(self):
		pos = self.mark()
		if (True and
		 (n0 := self.decorators()) is not None and
		 (n1 := self.function_def_raw()) is not None and
		   True):
			return function_def0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.function_def_raw()) is not None and
		   True):
			return function_def1(n2)
		self.reset(pos)
		return None
	@memoize
	def function_def_raw(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('def') is not None and
		 (n1 := self.expect('NAME')) is not None and
		 self.expect('(') is not None and
		 ((n3 := self.synthetic_rule_73()) or True) is not None and
		 self.expect(')') is not None and
		 ((n5 := self.synthetic_rule_74()) or True) is not None and
		 self.expect(':') is not None and
		 ((n7 := self.synthetic_rule_75()) or True) is not None and
		 (n8 := self.block()) is not None and
		   True):
			return function_def_raw0(n1, n3, n5, n7, n8)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n9 := self.expect('ASYNC')) is not None and
		 self.expect_keyword('def') is not None and
		 (n11 := self.expect('NAME')) is not None and
		 self.expect('(') is not None and
		 ((n13 := self.synthetic_rule_76()) or True) is not None and
		 self.expect(')') is not None and
		 ((n15 := self.synthetic_rule_77()) or True) is not None and
		 self.expect(':') is not None and
		 ((n17 := self.synthetic_rule_78()) or True) is not None and
		 (n18 := self.block()) is not None and
		   True):
			return function_def_raw1(n9, n11, n13, n15, n17, n18)
		self.reset(pos)
		return None
	@memoize
	def func_type_comment(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NEWLINE')) is not None and
		 (n1 := self.expect('TYPE_COMMENT')) is not None and
		 self.positive_lookahead(self.synthetic_rule_79, ) is not None and
		   True):
			return func_type_comment0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.expect('TYPE_COMMENT')) is not None and
		   True):
			return func_type_comment1(n3)
		self.reset(pos)
		return None
	@memoize
	def params(self):
		pos = self.mark()
		if (True and
		 (n0 := self.parameters()) is not None and
		   True):
			return params0(n0)
		self.reset(pos)
		return None
	@memoize
	def parameters(self):
		pos = self.mark()
		if (True and
		 (n0 := self.slash_no_default()) is not None and
		 (n1 := self.loop(False, self.param_no_default, )) is not None and
		 (n2 := self.loop(False, self.param_with_default, )) is not None and
		 ((n3 := self.synthetic_rule_80()) or True) is not None and
		   True):
			return parameters0(n0, n1, n2, n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.slash_with_default()) is not None and
		 (n5 := self.loop(False, self.param_with_default, )) is not None and
		 ((n6 := self.synthetic_rule_81()) or True) is not None and
		   True):
			return parameters1(n4, n5, n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n7 := self.loop(True, self.param_no_default, )) is not None and
		 (n8 := self.loop(False, self.param_with_default, )) is not None and
		 ((n9 := self.synthetic_rule_82()) or True) is not None and
		   True):
			return parameters2(n7, n8, n9)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n10 := self.loop(True, self.param_with_default, )) is not None and
		 ((n11 := self.synthetic_rule_83()) or True) is not None and
		   True):
			return parameters3(n10, n11)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n12 := self.star_etc()) is not None and
		   True):
			return parameters4(n12)
		self.reset(pos)
		return None
	@memoize
	def slash_no_default(self):
		pos = self.mark()
		if (True and
		 (n0 := self.loop(True, self.param_no_default, )) is not None and
		 self.expect('/') is not None and
		 self.expect(',') is not None and
		   True):
			return slash_no_default0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.loop(True, self.param_no_default, )) is not None and
		 self.expect('/') is not None and
		 self.positive_lookahead(self.expect, ')') is not None and
		   True):
			return slash_no_default1(n3)
		self.reset(pos)
		return None
	@memoize
	def slash_with_default(self):
		pos = self.mark()
		if (True and
		 (n0 := self.loop(False, self.param_no_default, )) is not None and
		 (n1 := self.loop(True, self.param_with_default, )) is not None and
		 self.expect('/') is not None and
		 self.expect(',') is not None and
		   True):
			return slash_with_default0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.loop(False, self.param_no_default, )) is not None and
		 (n5 := self.loop(True, self.param_with_default, )) is not None and
		 self.expect('/') is not None and
		 self.positive_lookahead(self.expect, ')') is not None and
		   True):
			return slash_with_default1(n4, n5)
		self.reset(pos)
		return None
	@memoize
	def star_etc(self):
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 (n1 := self.param_no_default()) is not None and
		 (n2 := self.loop(False, self.param_maybe_default, )) is not None and
		 ((n3 := self.synthetic_rule_84()) or True) is not None and
		   True):
			return star_etc0(n1, n2, n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 self.expect(',') is not None and
		 (n6 := self.loop(True, self.param_maybe_default, )) is not None and
		 ((n7 := self.synthetic_rule_85()) or True) is not None and
		   True):
			return star_etc1(n6, n7)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n8 := self.kwds()) is not None and
		   True):
			return star_etc2(n8)
		self.reset(pos)
		return None
	@memoize
	def kwds(self):
		pos = self.mark()
		if (True and
		 self.expect('**') is not None and
		 (n1 := self.param_no_default()) is not None and
		   True):
			return kwds0(n1)
		self.reset(pos)
		return None
	@memoize
	def param_no_default(self):
		pos = self.mark()
		if (True and
		 (n0 := self.param()) is not None and
		 self.expect(',') is not None and
		 ((n2 := self.expect('TYPE_COMMENT')) or True) is not None and
		   True):
			return param_no_default0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.param()) is not None and
		 ((n4 := self.expect('TYPE_COMMENT')) or True) is not None and
		 self.positive_lookahead(self.expect, ')') is not None and
		   True):
			return param_no_default1(n3, n4)
		self.reset(pos)
		return None
	@memoize
	def param_with_default(self):
		pos = self.mark()
		if (True and
		 (n0 := self.param()) is not None and
		 (n1 := self.default()) is not None and
		 self.expect(',') is not None and
		 ((n3 := self.expect('TYPE_COMMENT')) or True) is not None and
		   True):
			return param_with_default0(n0, n1, n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.param()) is not None and
		 (n5 := self.default()) is not None and
		 ((n6 := self.expect('TYPE_COMMENT')) or True) is not None and
		 self.positive_lookahead(self.expect, ')') is not None and
		   True):
			return param_with_default1(n4, n5, n6)
		self.reset(pos)
		return None
	@memoize
	def param_maybe_default(self):
		pos = self.mark()
		if (True and
		 (n0 := self.param()) is not None and
		 ((n1 := self.default()) or True) is not None and
		 self.expect(',') is not None and
		 ((n3 := self.expect('TYPE_COMMENT')) or True) is not None and
		   True):
			return param_maybe_default0(n0, n1, n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.param()) is not None and
		 ((n5 := self.default()) or True) is not None and
		 ((n6 := self.expect('TYPE_COMMENT')) or True) is not None and
		 self.positive_lookahead(self.expect, ')') is not None and
		   True):
			return param_maybe_default1(n4, n5, n6)
		self.reset(pos)
		return None
	@memoize
	def param(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		 ((n1 := self.annotation()) or True) is not None and
		   True):
			return param0(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def annotation(self):
		pos = self.mark()
		if (True and
		 self.expect(':') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return annotation0(n1)
		self.reset(pos)
		return None
	@memoize
	def default(self):
		pos = self.mark()
		if (True and
		 self.expect('=') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return default0(n1)
		self.reset(pos)
		return None
	@memoize
	def decorators(self):
		pos = self.mark()
		if (True and
		 (n0 := self.loop(True, self.synthetic_rule_86, )) is not None and
		   True):
			return decorators0(n0)
		self.reset(pos)
		return None
	@memoize
	def class_def(self):
		pos = self.mark()
		if (True and
		 (n0 := self.decorators()) is not None and
		 (n1 := self.class_def_raw()) is not None and
		   True):
			return class_def0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.class_def_raw()) is not None and
		   True):
			return class_def1(n2)
		self.reset(pos)
		return None
	@memoize
	def class_def_raw(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('class') is not None and
		 (n1 := self.expect('NAME')) is not None and
		 ((n2 := self.synthetic_rule_88()) or True) is not None and
		 self.expect(':') is not None and
		 (n4 := self.block()) is not None and
		   True):
			return class_def_raw0(n1, n2, n4)
		self.reset(pos)
		return None
	@memoize
	def block(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NEWLINE')) is not None and
		 (n1 := self.expect('INDENT')) is not None and
		 (n2 := self.statements()) is not None and
		 (n3 := self.expect('DEDENT')) is not None and
		   True):
			return block0(n0, n1, n2, n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.simple_stmts()) is not None and
		   True):
			return block1(n4)
		self.reset(pos)
		return None
	@memoize
	def star_expressions(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_expression()) is not None and
		 (n1 := self.loop(True, self.synthetic_rule_89, )) is not None and
		 ((n2 := self.synthetic_rule_90()) or True) is not None and
		   True):
			return star_expressions0(n0, n1, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.star_expression()) is not None and
		 self.expect(',') is not None and
		   True):
			return star_expressions1(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n5 := self.star_expression()) is not None and
		   True):
			return star_expressions2(n5)
		self.reset(pos)
		return None
	@memoize
	def star_expression(self):
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return star_expression0(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.expression()) is not None and
		   True):
			return star_expression1(n2)
		self.reset(pos)
		return None
	@memoize
	def star_named_expressions(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_92()) is not None and
		 ((n1 := self.synthetic_rule_93()) or True) is not None and
		   True):
			return star_named_expressions0(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def star_named_expression(self):
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return star_named_expression0(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.named_expression()) is not None and
		   True):
			return star_named_expression1(n2)
		self.reset(pos)
		return None
	@memoize
	def assignment_expression(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		 self.expect(':=') is not None and
		 True is not None and
		 (n3 := self.expression()) is not None and
		   True):
			return assignment_expression0(n0, n3)
		self.reset(pos)
		return None
	@memoize
	def named_expression(self):
		pos = self.mark()
		if (True and
		 (n0 := self.assignment_expression()) is not None and
		   True):
			return named_expression0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.expression()) is not None and
		 self.negative_lookahead(self.expect, ':=') is not None and
		   True):
			return named_expression1(n1)
		self.reset(pos)
		return None
	@memoize
	def annotated_rhs(self):
		pos = self.mark()
		if (True and
		 (n0 := self.yield_expr()) is not None and
		   True):
			return annotated_rhs0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.star_expressions()) is not None and
		   True):
			return annotated_rhs1(n1)
		self.reset(pos)
		return None
	@memoize
	def expressions(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		 (n1 := self.loop(True, self.synthetic_rule_94, )) is not None and
		 ((n2 := self.synthetic_rule_95()) or True) is not None and
		   True):
			return expressions0(n0, n1, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.expression()) is not None and
		 self.expect(',') is not None and
		   True):
			return expressions1(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n5 := self.expression()) is not None and
		   True):
			return expressions2(n5)
		self.reset(pos)
		return None
	@memoize
	def expression(self):
		pos = self.mark()
		if (True and
		 (n0 := self.disjunction()) is not None and
		 self.expect_keyword('if') is not None and
		 (n2 := self.disjunction()) is not None and
		 self.expect_keyword('else') is not None and
		 (n4 := self.expression()) is not None and
		   True):
			return expression0(n0, n2, n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n5 := self.disjunction()) is not None and
		   True):
			return expression1(n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.lambdef()) is not None and
		   True):
			return expression2(n6)
		self.reset(pos)
		return None
	@memoize
	def lambdef(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('lambda') is not None and
		 ((n1 := self.synthetic_rule_96()) or True) is not None and
		 self.expect(':') is not None and
		 (n3 := self.expression()) is not None and
		   True):
			return lambdef0(n1, n3)
		self.reset(pos)
		return None
	@memoize
	def lambda_params(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_parameters()) is not None and
		   True):
			return lambda_params0(n0)
		self.reset(pos)
		return None
	@memoize
	def lambda_parameters(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_slash_no_default()) is not None and
		 (n1 := self.loop(False, self.lambda_param_no_default, )) is not None and
		 (n2 := self.loop(False, self.lambda_param_with_default, )) is not None and
		 ((n3 := self.synthetic_rule_97()) or True) is not None and
		   True):
			return lambda_parameters0(n0, n1, n2, n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.lambda_slash_with_default()) is not None and
		 (n5 := self.loop(False, self.lambda_param_with_default, )) is not None and
		 ((n6 := self.synthetic_rule_98()) or True) is not None and
		   True):
			return lambda_parameters1(n4, n5, n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n7 := self.loop(True, self.lambda_param_no_default, )) is not None and
		 (n8 := self.loop(False, self.lambda_param_with_default, )) is not None and
		 ((n9 := self.synthetic_rule_99()) or True) is not None and
		   True):
			return lambda_parameters2(n7, n8, n9)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n10 := self.loop(True, self.lambda_param_with_default, )) is not None and
		 ((n11 := self.synthetic_rule_100()) or True) is not None and
		   True):
			return lambda_parameters3(n10, n11)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n12 := self.lambda_star_etc()) is not None and
		   True):
			return lambda_parameters4(n12)
		self.reset(pos)
		return None
	@memoize
	def lambda_slash_no_default(self):
		pos = self.mark()
		if (True and
		 (n0 := self.loop(True, self.lambda_param_no_default, )) is not None and
		 self.expect('/') is not None and
		 self.expect(',') is not None and
		   True):
			return lambda_slash_no_default0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.loop(True, self.lambda_param_no_default, )) is not None and
		 self.expect('/') is not None and
		 self.positive_lookahead(self.expect, ':') is not None and
		   True):
			return lambda_slash_no_default1(n3)
		self.reset(pos)
		return None
	@memoize
	def lambda_slash_with_default(self):
		pos = self.mark()
		if (True and
		 (n0 := self.loop(False, self.lambda_param_no_default, )) is not None and
		 (n1 := self.loop(True, self.lambda_param_with_default, )) is not None and
		 self.expect('/') is not None and
		 self.expect(',') is not None and
		   True):
			return lambda_slash_with_default0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.loop(False, self.lambda_param_no_default, )) is not None and
		 (n5 := self.loop(True, self.lambda_param_with_default, )) is not None and
		 self.expect('/') is not None and
		 self.positive_lookahead(self.expect, ':') is not None and
		   True):
			return lambda_slash_with_default1(n4, n5)
		self.reset(pos)
		return None
	@memoize
	def lambda_star_etc(self):
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 (n1 := self.lambda_param_no_default()) is not None and
		 (n2 := self.loop(False, self.lambda_param_maybe_default, )) is not None and
		 ((n3 := self.synthetic_rule_101()) or True) is not None and
		   True):
			return lambda_star_etc0(n1, n2, n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 self.expect(',') is not None and
		 (n6 := self.loop(True, self.lambda_param_maybe_default, )) is not None and
		 ((n7 := self.synthetic_rule_102()) or True) is not None and
		   True):
			return lambda_star_etc1(n6, n7)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n8 := self.lambda_kwds()) is not None and
		   True):
			return lambda_star_etc2(n8)
		self.reset(pos)
		return None
	@memoize
	def lambda_kwds(self):
		pos = self.mark()
		if (True and
		 self.expect('**') is not None and
		 (n1 := self.lambda_param_no_default()) is not None and
		   True):
			return lambda_kwds0(n1)
		self.reset(pos)
		return None
	@memoize
	def lambda_param_no_default(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_param()) is not None and
		 self.expect(',') is not None and
		   True):
			return lambda_param_no_default0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.lambda_param()) is not None and
		 self.positive_lookahead(self.expect, ':') is not None and
		   True):
			return lambda_param_no_default1(n2)
		self.reset(pos)
		return None
	@memoize
	def lambda_param_with_default(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_param()) is not None and
		 (n1 := self.default()) is not None and
		 self.expect(',') is not None and
		   True):
			return lambda_param_with_default0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.lambda_param()) is not None and
		 (n4 := self.default()) is not None and
		 self.positive_lookahead(self.expect, ':') is not None and
		   True):
			return lambda_param_with_default1(n3, n4)
		self.reset(pos)
		return None
	@memoize
	def lambda_param_maybe_default(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_param()) is not None and
		 ((n1 := self.default()) or True) is not None and
		 self.expect(',') is not None and
		   True):
			return lambda_param_maybe_default0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.lambda_param()) is not None and
		 ((n4 := self.default()) or True) is not None and
		 self.positive_lookahead(self.expect, ':') is not None and
		   True):
			return lambda_param_maybe_default1(n3, n4)
		self.reset(pos)
		return None
	@memoize
	def lambda_param(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		   True):
			return lambda_param0(n0)
		self.reset(pos)
		return None
	@memoize
	def disjunction(self):
		pos = self.mark()
		if (True and
		 (n0 := self.conjunction()) is not None and
		 (n1 := self.loop(True, self.synthetic_rule_103, )) is not None and
		   True):
			return disjunction0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.conjunction()) is not None and
		   True):
			return disjunction1(n2)
		self.reset(pos)
		return None
	@memoize
	def conjunction(self):
		pos = self.mark()
		if (True and
		 (n0 := self.inversion()) is not None and
		 (n1 := self.loop(True, self.synthetic_rule_104, )) is not None and
		   True):
			return conjunction0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.inversion()) is not None and
		   True):
			return conjunction1(n2)
		self.reset(pos)
		return None
	@memoize
	def inversion(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('not') is not None and
		 (n1 := self.inversion()) is not None and
		   True):
			return inversion0(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.comparison()) is not None and
		   True):
			return inversion1(n2)
		self.reset(pos)
		return None
	@memoize
	def comparison(self):
		pos = self.mark()
		if (True and
		 (n0 := self.bitwise_or()) is not None and
		 (n1 := self.loop(True, self.compare_op_bitwise_or_pair, )) is not None and
		   True):
			return comparison0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.bitwise_or()) is not None and
		   True):
			return comparison1(n2)
		self.reset(pos)
		return None
	@memoize
	def compare_op_bitwise_or_pair(self):
		pos = self.mark()
		if (True and
		 (n0 := self.eq_bitwise_or()) is not None and
		   True):
			return compare_op_bitwise_or_pair0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.noteq_bitwise_or()) is not None and
		   True):
			return compare_op_bitwise_or_pair1(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.lte_bitwise_or()) is not None and
		   True):
			return compare_op_bitwise_or_pair2(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.lt_bitwise_or()) is not None and
		   True):
			return compare_op_bitwise_or_pair3(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.gte_bitwise_or()) is not None and
		   True):
			return compare_op_bitwise_or_pair4(n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n5 := self.gt_bitwise_or()) is not None and
		   True):
			return compare_op_bitwise_or_pair5(n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.notin_bitwise_or()) is not None and
		   True):
			return compare_op_bitwise_or_pair6(n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n7 := self.in_bitwise_or()) is not None and
		   True):
			return compare_op_bitwise_or_pair7(n7)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n8 := self.isnot_bitwise_or()) is not None and
		   True):
			return compare_op_bitwise_or_pair8(n8)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n9 := self.is_bitwise_or()) is not None and
		   True):
			return compare_op_bitwise_or_pair9(n9)
		self.reset(pos)
		return None
	@memoize
	def eq_bitwise_or(self):
		pos = self.mark()
		if (True and
		 self.expect('==') is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return eq_bitwise_or0(n1)
		self.reset(pos)
		return None
	@memoize
	def noteq_bitwise_or(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_105()) is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return noteq_bitwise_or0(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def lte_bitwise_or(self):
		pos = self.mark()
		if (True and
		 self.expect('<=') is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return lte_bitwise_or0(n1)
		self.reset(pos)
		return None
	@memoize
	def lt_bitwise_or(self):
		pos = self.mark()
		if (True and
		 self.expect('<') is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return lt_bitwise_or0(n1)
		self.reset(pos)
		return None
	@memoize
	def gte_bitwise_or(self):
		pos = self.mark()
		if (True and
		 self.expect('>=') is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return gte_bitwise_or0(n1)
		self.reset(pos)
		return None
	@memoize
	def gt_bitwise_or(self):
		pos = self.mark()
		if (True and
		 self.expect('>') is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return gt_bitwise_or0(n1)
		self.reset(pos)
		return None
	@memoize
	def notin_bitwise_or(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('not') is not None and
		 self.expect_keyword('in') is not None and
		 (n2 := self.bitwise_or()) is not None and
		   True):
			return notin_bitwise_or0(n2)
		self.reset(pos)
		return None
	@memoize
	def in_bitwise_or(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('in') is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return in_bitwise_or0(n1)
		self.reset(pos)
		return None
	@memoize
	def isnot_bitwise_or(self):
		pos = self.mark()
		if (True and
		 self.expect('is') is not None and
		 self.expect_keyword('not') is not None and
		 (n2 := self.bitwise_or()) is not None and
		   True):
			return isnot_bitwise_or0(n2)
		self.reset(pos)
		return None
	@memoize
	def is_bitwise_or(self):
		pos = self.mark()
		if (True and
		 self.expect('is') is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return is_bitwise_or0(n1)
		self.reset(pos)
		return None
	@memoize_left_rec
	def bitwise_or(self):
		pos = self.mark()
		if (True and
		 (n0 := self.bitwise_or()) is not None and
		 self.expect('|') is not None and
		 (n2 := self.bitwise_xor()) is not None and
		   True):
			return bitwise_or0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.bitwise_xor()) is not None and
		   True):
			return bitwise_or1(n3)
		self.reset(pos)
		return None
	@memoize_left_rec
	def bitwise_xor(self):
		pos = self.mark()
		if (True and
		 (n0 := self.bitwise_xor()) is not None and
		 self.expect('^') is not None and
		 (n2 := self.bitwise_and()) is not None and
		   True):
			return bitwise_xor0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.bitwise_and()) is not None and
		   True):
			return bitwise_xor1(n3)
		self.reset(pos)
		return None
	@memoize_left_rec
	def bitwise_and(self):
		pos = self.mark()
		if (True and
		 (n0 := self.bitwise_and()) is not None and
		 self.expect('&') is not None and
		 (n2 := self.shift_expr()) is not None and
		   True):
			return bitwise_and0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.shift_expr()) is not None and
		   True):
			return bitwise_and1(n3)
		self.reset(pos)
		return None
	@memoize_left_rec
	def shift_expr(self):
		pos = self.mark()
		if (True and
		 (n0 := self.shift_expr()) is not None and
		 self.expect('<<') is not None and
		 (n2 := self.sum()) is not None and
		   True):
			return shift_expr0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.shift_expr()) is not None and
		 self.expect('>>') is not None and
		 (n5 := self.sum()) is not None and
		   True):
			return shift_expr1(n3, n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.sum()) is not None and
		   True):
			return shift_expr2(n6)
		self.reset(pos)
		return None
	@memoize_left_rec
	def sum(self):
		pos = self.mark()
		if (True and
		 (n0 := self.sum()) is not None and
		 self.expect('+') is not None and
		 (n2 := self.term()) is not None and
		   True):
			return sum0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.sum()) is not None and
		 self.expect('-') is not None and
		 (n5 := self.term()) is not None and
		   True):
			return sum1(n3, n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.term()) is not None and
		   True):
			return sum2(n6)
		self.reset(pos)
		return None
	@memoize_left_rec
	def term(self):
		pos = self.mark()
		if (True and
		 (n0 := self.term()) is not None and
		 self.expect('*') is not None and
		 (n2 := self.factor()) is not None and
		   True):
			return term0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.term()) is not None and
		 self.expect('/') is not None and
		 (n5 := self.factor()) is not None and
		   True):
			return term1(n3, n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.term()) is not None and
		 self.expect('//') is not None and
		 (n8 := self.factor()) is not None and
		   True):
			return term2(n6, n8)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n9 := self.term()) is not None and
		 self.expect('%') is not None and
		 (n11 := self.factor()) is not None and
		   True):
			return term3(n9, n11)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n12 := self.term()) is not None and
		 self.expect('@') is not None and
		 (n14 := self.factor()) is not None and
		   True):
			return term4(n12, n14)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n15 := self.factor()) is not None and
		   True):
			return term5(n15)
		self.reset(pos)
		return None
	@memoize
	def factor(self):
		pos = self.mark()
		if (True and
		 self.expect('+') is not None and
		 (n1 := self.factor()) is not None and
		   True):
			return factor0(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('-') is not None and
		 (n3 := self.factor()) is not None and
		   True):
			return factor1(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('~') is not None and
		 (n5 := self.factor()) is not None and
		   True):
			return factor2(n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.power()) is not None and
		   True):
			return factor3(n6)
		self.reset(pos)
		return None
	@memoize
	def power(self):
		pos = self.mark()
		if (True and
		 (n0 := self.await_primary()) is not None and
		 self.expect('**') is not None and
		 (n2 := self.factor()) is not None and
		   True):
			return power0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.await_primary()) is not None and
		   True):
			return power1(n3)
		self.reset(pos)
		return None
	@memoize
	def await_primary(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('AWAIT')) is not None and
		 (n1 := self.primary()) is not None and
		   True):
			return await_primary0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.primary()) is not None and
		   True):
			return await_primary1(n2)
		self.reset(pos)
		return None
	@memoize_left_rec
	def primary(self):
		pos = self.mark()
		if (True and
		 (n0 := self.primary()) is not None and
		 self.expect('.') is not None and
		 (n2 := self.expect('NAME')) is not None and
		   True):
			return primary0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.primary()) is not None and
		 (n4 := self.genexp()) is not None and
		   True):
			return primary1(n3, n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n5 := self.primary()) is not None and
		 self.expect('(') is not None and
		 ((n7 := self.synthetic_rule_106()) or True) is not None and
		 self.expect(')') is not None and
		   True):
			return primary2(n5, n7)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n9 := self.primary()) is not None and
		 self.expect('[') is not None and
		 (n11 := self.slices()) is not None and
		 self.expect(']') is not None and
		   True):
			return primary3(n9, n11)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n13 := self.atom()) is not None and
		   True):
			return primary4(n13)
		self.reset(pos)
		return None
	@memoize
	def slices(self):
		pos = self.mark()
		if (True and
		 (n0 := self.slice()) is not None and
		 self.negative_lookahead(self.expect, ',') is not None and
		   True):
			return slices0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.synthetic_rule_108()) is not None and
		 ((n3 := self.synthetic_rule_109()) or True) is not None and
		   True):
			return slices1(n2, n3)
		self.reset(pos)
		return None
	@memoize
	def slice(self):
		pos = self.mark()
		if (True and
		 ((n0 := self.synthetic_rule_110()) or True) is not None and
		 self.expect(':') is not None and
		 ((n2 := self.synthetic_rule_111()) or True) is not None and
		 ((n3 := self.synthetic_rule_113()) or True) is not None and
		   True):
			return slice0(n0, n2, n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.named_expression()) is not None and
		   True):
			return slice1(n4)
		self.reset(pos)
		return None
	@memoize
	def atom(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		   True):
			return atom0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('True') is not None and
		   True):
			return atom1()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('False') is not None and
		   True):
			return atom2()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('None') is not None and
		   True):
			return atom3()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.strings()) is not None and
		   True):
			return atom4(n4)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n5 := self.expect('NUMBER')) is not None and
		   True):
			return atom5(n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.synthetic_rule_114()) is not None and
		   True):
			return atom6(n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n7 := self.synthetic_rule_115()) is not None and
		   True):
			return atom7(n7)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n8 := self.synthetic_rule_116()) is not None and
		   True):
			return atom8(n8)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('...') is not None and
		   True):
			return atom9()
		self.reset(pos)
		return None
	@memoize
	def strings(self):
		pos = self.mark()
		if (True and
		 (n0 := self.loop(True, self.expect, 'STRING')) is not None and
		   True):
			return strings0(n0)
		self.reset(pos)
		return None
	@memoize
	def list(self):
		pos = self.mark()
		if (True and
		 self.expect('[') is not None and
		 ((n1 := self.synthetic_rule_117()) or True) is not None and
		 self.expect(']') is not None and
		   True):
			return list0(n1)
		self.reset(pos)
		return None
	@memoize
	def listcomp(self):
		pos = self.mark()
		if (True and
		 self.expect('[') is not None and
		 (n1 := self.named_expression()) is not None and
		 (n2 := self.for_if_clauses()) is not None and
		 self.expect(']') is not None and
		   True):
			return listcomp0(n1, n2)
		self.reset(pos)
		return None
	@memoize
	def tuple(self):
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 ((n1 := self.synthetic_rule_119()) or True) is not None and
		 self.expect(')') is not None and
		   True):
			return tuple0(n1)
		self.reset(pos)
		return None
	@memoize
	def group(self):
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 (n1 := self.synthetic_rule_120()) is not None and
		 self.expect(')') is not None and
		   True):
			return group0(n1)
		self.reset(pos)
		return None
	@memoize
	def genexp(self):
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 (n1 := self.synthetic_rule_121()) is not None and
		 (n2 := self.for_if_clauses()) is not None and
		 self.expect(')') is not None and
		   True):
			return genexp0(n1, n2)
		self.reset(pos)
		return None
	@memoize
	def set(self):
		pos = self.mark()
		if (True and
		 self.expect('{') is not None and
		 (n1 := self.star_named_expressions()) is not None and
		 self.expect('}') is not None and
		   True):
			return set0(n1)
		self.reset(pos)
		return None
	@memoize
	def setcomp(self):
		pos = self.mark()
		if (True and
		 self.expect('{') is not None and
		 (n1 := self.named_expression()) is not None and
		 (n2 := self.for_if_clauses()) is not None and
		 self.expect('}') is not None and
		   True):
			return setcomp0(n1, n2)
		self.reset(pos)
		return None
	@memoize
	def dict(self):
		pos = self.mark()
		if (True and
		 self.expect('{') is not None and
		 ((n1 := self.synthetic_rule_122()) or True) is not None and
		 self.expect('}') is not None and
		   True):
			return dict0(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('{') is not None and
		 (n4 := self.invalid_double_starred_kvpairs()) is not None and
		 self.expect('}') is not None and
		   True):
			return dict1(n4)
		self.reset(pos)
		return None
	@memoize
	def dictcomp(self):
		pos = self.mark()
		if (True and
		 self.expect('{') is not None and
		 (n1 := self.kvpair()) is not None and
		 (n2 := self.for_if_clauses()) is not None and
		 self.expect('}') is not None and
		   True):
			return dictcomp0(n1, n2)
		self.reset(pos)
		return None
	@memoize
	def double_starred_kvpairs(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_124()) is not None and
		 ((n1 := self.synthetic_rule_125()) or True) is not None and
		   True):
			return double_starred_kvpairs0(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def double_starred_kvpair(self):
		pos = self.mark()
		if (True and
		 self.expect('**') is not None and
		 (n1 := self.bitwise_or()) is not None and
		   True):
			return double_starred_kvpair0(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.kvpair()) is not None and
		   True):
			return double_starred_kvpair1(n2)
		self.reset(pos)
		return None
	@memoize
	def kvpair(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		 self.expect(':') is not None and
		 (n2 := self.expression()) is not None and
		   True):
			return kvpair0(n0, n2)
		self.reset(pos)
		return None
	@memoize
	def for_if_clauses(self):
		pos = self.mark()
		if (True and
		 (n0 := self.loop(True, self.for_if_clause, )) is not None and
		   True):
			return for_if_clauses0(n0)
		self.reset(pos)
		return None
	@memoize
	def for_if_clause(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('ASYNC')) is not None and
		 self.expect_keyword('for') is not None and
		 (n2 := self.star_targets()) is not None and
		 self.expect_keyword('in') is not None and
		 True is not None and
		 (n5 := self.disjunction()) is not None and
		 (n6 := self.loop(False, self.synthetic_rule_126, )) is not None and
		   True):
			return for_if_clause0(n0, n2, n5, n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('for') is not None and
		 (n8 := self.star_targets()) is not None and
		 self.expect_keyword('in') is not None and
		 True is not None and
		 (n11 := self.disjunction()) is not None and
		 (n12 := self.loop(False, self.synthetic_rule_127, )) is not None and
		   True):
			return for_if_clause1(n8, n11, n12)
		self.reset(pos)
		return None
	@memoize
	def yield_expr(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('yield') is not None and
		 self.expect_keyword('from') is not None and
		 (n2 := self.expression()) is not None and
		   True):
			return yield_expr0(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect_keyword('yield') is not None and
		 ((n4 := self.synthetic_rule_128()) or True) is not None and
		   True):
			return yield_expr1(n4)
		self.reset(pos)
		return None
	@memoize
	def arguments(self):
		pos = self.mark()
		if (True and
		 (n0 := self.args()) is not None and
		 ((n1 := self.synthetic_rule_129()) or True) is not None and
		 self.positive_lookahead(self.expect, ')') is not None and
		   True):
			return arguments0(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def args(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_133()) is not None and
		 ((n1 := self.synthetic_rule_134()) or True) is not None and
		   True):
			return args0(n0, n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.kwargs()) is not None and
		   True):
			return args1(n2)
		self.reset(pos)
		return None
	@memoize
	def kwargs(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_136()) is not None and
		 self.expect(',') is not None and
		 (n2 := self.synthetic_rule_138()) is not None and
		   True):
			return kwargs0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.synthetic_rule_140()) is not None and
		   True):
			return kwargs1(n3)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.synthetic_rule_142()) is not None and
		   True):
			return kwargs2(n4)
		self.reset(pos)
		return None
	@memoize
	def starred_expression(self):
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return starred_expression0(n1)
		self.reset(pos)
		return None
	@memoize
	def kwarg_or_starred(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		 self.expect('=') is not None and
		 (n2 := self.expression()) is not None and
		   True):
			return kwarg_or_starred0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.starred_expression()) is not None and
		   True):
			return kwarg_or_starred1(n3)
		self.reset(pos)
		return None
	@memoize
	def kwarg_or_double_starred(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		 self.expect('=') is not None and
		 (n2 := self.expression()) is not None and
		   True):
			return kwarg_or_double_starred0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('**') is not None and
		 (n4 := self.expression()) is not None and
		   True):
			return kwarg_or_double_starred1(n4)
		self.reset(pos)
		return None
	@memoize
	def star_targets(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_target()) is not None and
		 self.negative_lookahead(self.expect, ',') is not None and
		   True):
			return star_targets0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.star_target()) is not None and
		 (n3 := self.loop(False, self.synthetic_rule_143, )) is not None and
		 ((n4 := self.synthetic_rule_144()) or True) is not None and
		   True):
			return star_targets1(n2, n3, n4)
		self.reset(pos)
		return None
	@memoize
	def star_targets_list_seq(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_146()) is not None and
		 ((n1 := self.synthetic_rule_147()) or True) is not None and
		   True):
			return star_targets_list_seq0(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def star_targets_tuple_seq(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_target()) is not None and
		 (n1 := self.loop(True, self.synthetic_rule_148, )) is not None and
		 ((n2 := self.synthetic_rule_149()) or True) is not None and
		   True):
			return star_targets_tuple_seq0(n0, n1, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.star_target()) is not None and
		 self.expect(',') is not None and
		   True):
			return star_targets_tuple_seq1(n3)
		self.reset(pos)
		return None
	@memoize
	def star_target(self):
		pos = self.mark()
		if (True and
		 self.expect('*') is not None and
		 (n1 := self.synthetic_rule_150()) is not None and
		   True):
			return star_target0(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.target_with_star_atom()) is not None and
		   True):
			return star_target1(n2)
		self.reset(pos)
		return None
	@memoize
	def target_with_star_atom(self):
		pos = self.mark()
		if (True and
		 (n0 := self.t_primary()) is not None and
		 self.expect('.') is not None and
		 (n2 := self.expect('NAME')) is not None and
		 self.negative_lookahead(self.t_lookahead, ) is not None and
		   True):
			return target_with_star_atom0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.t_primary()) is not None and
		 self.expect('[') is not None and
		 (n6 := self.slices()) is not None and
		 self.expect(']') is not None and
		 self.negative_lookahead(self.t_lookahead, ) is not None and
		   True):
			return target_with_star_atom1(n4, n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n9 := self.star_atom()) is not None and
		   True):
			return target_with_star_atom2(n9)
		self.reset(pos)
		return None
	@memoize
	def star_atom(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		   True):
			return star_atom0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 (n2 := self.target_with_star_atom()) is not None and
		 self.expect(')') is not None and
		   True):
			return star_atom1(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 ((n5 := self.synthetic_rule_151()) or True) is not None and
		 self.expect(')') is not None and
		   True):
			return star_atom2(n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('[') is not None and
		 ((n8 := self.synthetic_rule_152()) or True) is not None and
		 self.expect(']') is not None and
		   True):
			return star_atom3(n8)
		self.reset(pos)
		return None
	@memoize
	def single_target(self):
		pos = self.mark()
		if (True and
		 (n0 := self.single_subscript_attribute_target()) is not None and
		   True):
			return single_target0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.expect('NAME')) is not None and
		   True):
			return single_target1(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 (n3 := self.single_target()) is not None and
		 self.expect(')') is not None and
		   True):
			return single_target2(n3)
		self.reset(pos)
		return None
	@memoize
	def single_subscript_attribute_target(self):
		pos = self.mark()
		if (True and
		 (n0 := self.t_primary()) is not None and
		 self.expect('.') is not None and
		 (n2 := self.expect('NAME')) is not None and
		 self.negative_lookahead(self.t_lookahead, ) is not None and
		   True):
			return single_subscript_attribute_target0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.t_primary()) is not None and
		 self.expect('[') is not None and
		 (n6 := self.slices()) is not None and
		 self.expect(']') is not None and
		 self.negative_lookahead(self.t_lookahead, ) is not None and
		   True):
			return single_subscript_attribute_target1(n4, n6)
		self.reset(pos)
		return None
	@memoize
	def del_targets(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_154()) is not None and
		 ((n1 := self.synthetic_rule_155()) or True) is not None and
		   True):
			return del_targets0(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def del_target(self):
		pos = self.mark()
		if (True and
		 (n0 := self.t_primary()) is not None and
		 self.expect('.') is not None and
		 (n2 := self.expect('NAME')) is not None and
		 self.negative_lookahead(self.t_lookahead, ) is not None and
		   True):
			return del_target0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.t_primary()) is not None and
		 self.expect('[') is not None and
		 (n6 := self.slices()) is not None and
		 self.expect(']') is not None and
		 self.negative_lookahead(self.t_lookahead, ) is not None and
		   True):
			return del_target1(n4, n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n9 := self.del_t_atom()) is not None and
		   True):
			return del_target2(n9)
		self.reset(pos)
		return None
	@memoize
	def del_t_atom(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		   True):
			return del_t_atom0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 (n2 := self.del_target()) is not None and
		 self.expect(')') is not None and
		   True):
			return del_t_atom1(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 ((n5 := self.synthetic_rule_156()) or True) is not None and
		 self.expect(')') is not None and
		   True):
			return del_t_atom2(n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('[') is not None and
		 ((n8 := self.synthetic_rule_157()) or True) is not None and
		 self.expect(']') is not None and
		   True):
			return del_t_atom3(n8)
		self.reset(pos)
		return None
	@memoize_left_rec
	def t_primary(self):
		pos = self.mark()
		if (True and
		 (n0 := self.t_primary()) is not None and
		 self.expect('.') is not None and
		 (n2 := self.expect('NAME')) is not None and
		 self.positive_lookahead(self.t_lookahead, ) is not None and
		   True):
			return t_primary0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n4 := self.t_primary()) is not None and
		 self.expect('[') is not None and
		 (n6 := self.slices()) is not None and
		 self.expect(']') is not None and
		 self.positive_lookahead(self.t_lookahead, ) is not None and
		   True):
			return t_primary1(n4, n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n9 := self.t_primary()) is not None and
		 (n10 := self.genexp()) is not None and
		 self.positive_lookahead(self.t_lookahead, ) is not None and
		   True):
			return t_primary2(n9, n10)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n12 := self.t_primary()) is not None and
		 self.expect('(') is not None and
		 ((n14 := self.synthetic_rule_158()) or True) is not None and
		 self.expect(')') is not None and
		 self.positive_lookahead(self.t_lookahead, ) is not None and
		   True):
			return t_primary3(n12, n14)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n17 := self.atom()) is not None and
		 self.positive_lookahead(self.t_lookahead, ) is not None and
		   True):
			return t_primary4(n17)
		self.reset(pos)
		return None
	@memoize
	def t_lookahead(self):
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		   True):
			return t_lookahead0()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('[') is not None and
		   True):
			return t_lookahead1()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('.') is not None and
		   True):
			return t_lookahead2()
		self.reset(pos)
		return None
	@memoize
	def invalid_double_starred_kvpairs(self):
		pos = self.mark()
		if (True and
		 (n0 := self.synthetic_rule_160()) is not None and
		 self.expect(',') is not None and
		 (n2 := self.invalid_kvpair()) is not None and
		   True):
			return invalid_double_starred_kvpairs0(n0, n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.expression()) is not None and
		 self.expect(':') is not None and
		 self.expect('*') is not None and
		 (n6 := self.bitwise_or()) is not None and
		   True):
			return invalid_double_starred_kvpairs1(n3, n6)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n7 := self.expression()) is not None and
		 self.expect(':') is not None and
		 self.positive_lookahead(self.synthetic_rule_161, ) is not None and
		   True):
			return invalid_double_starred_kvpairs2(n7)
		self.reset(pos)
		return None
	@memoize
	def invalid_kvpair(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		 self.negative_lookahead(self.synthetic_rule_162, ) is not None and
		   True):
			return invalid_kvpair0(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.expression()) is not None and
		 self.expect(':') is not None and
		 self.expect('*') is not None and
		 (n5 := self.bitwise_or()) is not None and
		   True):
			return invalid_kvpair1(n2, n5)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n6 := self.expression()) is not None and
		 self.expect(':') is not None and
		   True):
			return invalid_kvpair2(n6)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_0(self):
		pos = self.mark()
		if (True and
		 (n0 := self.statements()) is not None and
		   True):
			return synthetic_rule_0(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_1(self):
		pos = self.mark()
		if (True and
		 (n0 := self.type_expressions()) is not None and
		   True):
			return synthetic_rule_1(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_2(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return synthetic_rule_2(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_3(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_2, )) is not None and
		   True):
			return synthetic_rule_3(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_4(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return synthetic_rule_4(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_5(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_4, )) is not None and
		   True):
			return synthetic_rule_5(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_6(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return synthetic_rule_6(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_7(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_6, )) is not None and
		   True):
			return synthetic_rule_7(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_8(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return synthetic_rule_8(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_9(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_8, )) is not None and
		   True):
			return synthetic_rule_9(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_10(self):
		pos = self.mark()
		if (True and
		 self.expect(';') is not None and
		 (n1 := self.simple_stmt()) is not None and
		   True):
			return synthetic_rule_10(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_11(self):
		pos = self.mark()
		if (True and
		 (n0 := self.simple_stmt()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_10, )) is not None and
		   True):
			return synthetic_rule_11(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_12(self):
		pos = self.mark()
		if (True and
		 self.expect(';') is not None and
		   True):
			return synthetic_rule_12()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_13(self):
		pos = self.mark()
		if (True and
		 self.expect('=') is not None and
		 (n1 := self.annotated_rhs()) is not None and
		   True):
			return synthetic_rule_13(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_14(self):
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 (n1 := self.single_target()) is not None and
		 self.expect(')') is not None and
		   True):
			return synthetic_rule_14(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.single_subscript_attribute_target()) is not None and
		   True):
			return synthetic_rule_14(n3)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_15(self):
		pos = self.mark()
		if (True and
		 self.expect('=') is not None and
		 (n1 := self.annotated_rhs()) is not None and
		   True):
			return synthetic_rule_15(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_16(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_targets()) is not None and
		 self.expect('=') is not None and
		   True):
			return synthetic_rule_16(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_17(self):
		pos = self.mark()
		if (True and
		 (n0 := self.yield_expr()) is not None and
		   True):
			return synthetic_rule_17(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.star_expressions()) is not None and
		   True):
			return synthetic_rule_17(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_18(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('TYPE_COMMENT')) is not None and
		   True):
			return synthetic_rule_18(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_19(self):
		pos = self.mark()
		if (True and
		 (n0 := self.yield_expr()) is not None and
		   True):
			return synthetic_rule_19(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.star_expressions()) is not None and
		   True):
			return synthetic_rule_19(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_20(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.expect('NAME')) is not None and
		   True):
			return synthetic_rule_20(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_21(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_20, )) is not None and
		   True):
			return synthetic_rule_21(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_22(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.expect('NAME')) is not None and
		   True):
			return synthetic_rule_22(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_23(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NAME')) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_22, )) is not None and
		   True):
			return synthetic_rule_23(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_24(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return synthetic_rule_24(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_25(self):
		pos = self.mark()
		if (True and
		 self.expect(';') is not None and
		   True):
			return synthetic_rule_25()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.expect('NEWLINE')) is not None and
		   True):
			return synthetic_rule_25(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_26(self):
		pos = self.mark()
		if (True and
		 self.expect('.') is not None and
		   True):
			return synthetic_rule_26()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('...') is not None and
		   True):
			return synthetic_rule_26()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_27(self):
		pos = self.mark()
		if (True and
		 self.expect('.') is not None and
		   True):
			return synthetic_rule_27()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('...') is not None and
		   True):
			return synthetic_rule_27()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_28(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_28()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_29(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.import_from_as_name()) is not None and
		   True):
			return synthetic_rule_29(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_30(self):
		pos = self.mark()
		if (True and
		 (n0 := self.import_from_as_name()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_29, )) is not None and
		   True):
			return synthetic_rule_30(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_31(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('as') is not None and
		 (n1 := self.expect('NAME')) is not None and
		   True):
			return synthetic_rule_31(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_32(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.dotted_as_name()) is not None and
		   True):
			return synthetic_rule_32(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_33(self):
		pos = self.mark()
		if (True and
		 (n0 := self.dotted_as_name()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_32, )) is not None and
		   True):
			return synthetic_rule_33(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_34(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('as') is not None and
		 (n1 := self.expect('NAME')) is not None and
		   True):
			return synthetic_rule_34(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_35(self):
		pos = self.mark()
		if (True and
		 (n0 := self.else_block()) is not None and
		   True):
			return synthetic_rule_35(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_36(self):
		pos = self.mark()
		if (True and
		 (n0 := self.else_block()) is not None and
		   True):
			return synthetic_rule_36(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_37(self):
		pos = self.mark()
		if (True and
		 (n0 := self.else_block()) is not None and
		   True):
			return synthetic_rule_37(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_38(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('TYPE_COMMENT')) is not None and
		   True):
			return synthetic_rule_38(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_39(self):
		pos = self.mark()
		if (True and
		 (n0 := self.else_block()) is not None and
		   True):
			return synthetic_rule_39(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_40(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('TYPE_COMMENT')) is not None and
		   True):
			return synthetic_rule_40(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_41(self):
		pos = self.mark()
		if (True and
		 (n0 := self.else_block()) is not None and
		   True):
			return synthetic_rule_41(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_42(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.with_item()) is not None and
		   True):
			return synthetic_rule_42(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_43(self):
		pos = self.mark()
		if (True and
		 (n0 := self.with_item()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_42, )) is not None and
		   True):
			return synthetic_rule_43(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_44(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.with_item()) is not None and
		   True):
			return synthetic_rule_44(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_45(self):
		pos = self.mark()
		if (True and
		 (n0 := self.with_item()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_44, )) is not None and
		   True):
			return synthetic_rule_45(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_46(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('TYPE_COMMENT')) is not None and
		   True):
			return synthetic_rule_46(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_47(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.with_item()) is not None and
		   True):
			return synthetic_rule_47(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_48(self):
		pos = self.mark()
		if (True and
		 (n0 := self.with_item()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_47, )) is not None and
		   True):
			return synthetic_rule_48(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_49(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.with_item()) is not None and
		   True):
			return synthetic_rule_49(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_50(self):
		pos = self.mark()
		if (True and
		 (n0 := self.with_item()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_49, )) is not None and
		   True):
			return synthetic_rule_50(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_51(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('TYPE_COMMENT')) is not None and
		   True):
			return synthetic_rule_51(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_52(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_52()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect(')') is not None and
		   True):
			return synthetic_rule_52()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect(':') is not None and
		   True):
			return synthetic_rule_52()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_53(self):
		pos = self.mark()
		if (True and
		 (n0 := self.else_block()) is not None and
		   True):
			return synthetic_rule_53(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_54(self):
		pos = self.mark()
		if (True and
		 (n0 := self.finally_block()) is not None and
		   True):
			return synthetic_rule_54(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_55(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('as') is not None and
		 (n1 := self.expect('NAME')) is not None and
		   True):
			return synthetic_rule_55(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_56(self):
		pos = self.mark()
		if (True and
		 self.expect('|') is not None and
		 (n1 := self.closed_pattern()) is not None and
		   True):
			return synthetic_rule_56(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_57(self):
		pos = self.mark()
		if (True and
		 (n0 := self.closed_pattern()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_56, )) is not None and
		   True):
			return synthetic_rule_57(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_58(self):
		pos = self.mark()
		if (True and
		 self.expect('+') is not None and
		   True):
			return synthetic_rule_58()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('-') is not None and
		   True):
			return synthetic_rule_58()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_59(self):
		pos = self.mark()
		if (True and
		 self.expect('+') is not None and
		   True):
			return synthetic_rule_59()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('-') is not None and
		   True):
			return synthetic_rule_59()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_60(self):
		pos = self.mark()
		if (True and
		 self.expect('.') is not None and
		   True):
			return synthetic_rule_60()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		   True):
			return synthetic_rule_60()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('=') is not None and
		   True):
			return synthetic_rule_60()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_61(self):
		pos = self.mark()
		if (True and
		 self.expect('.') is not None and
		   True):
			return synthetic_rule_61()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		   True):
			return synthetic_rule_61()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect('=') is not None and
		   True):
			return synthetic_rule_61()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_62(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.maybe_star_pattern()) is not None and
		   True):
			return synthetic_rule_62(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_63(self):
		pos = self.mark()
		if (True and
		 (n0 := self.maybe_star_pattern()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_62, )) is not None and
		   True):
			return synthetic_rule_63(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_64(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.key_value_pattern()) is not None and
		   True):
			return synthetic_rule_64(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_65(self):
		pos = self.mark()
		if (True and
		 (n0 := self.key_value_pattern()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_64, )) is not None and
		   True):
			return synthetic_rule_65(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_66(self):
		pos = self.mark()
		if (True and
		 (n0 := self.literal_expr()) is not None and
		   True):
			return synthetic_rule_66(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.attr()) is not None and
		   True):
			return synthetic_rule_66(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_67(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.pattern()) is not None and
		   True):
			return synthetic_rule_67(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_68(self):
		pos = self.mark()
		if (True and
		 (n0 := self.pattern()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_67, )) is not None and
		   True):
			return synthetic_rule_68(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_69(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.keyword_pattern()) is not None and
		   True):
			return synthetic_rule_69(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_70(self):
		pos = self.mark()
		if (True and
		 (n0 := self.keyword_pattern()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_69, )) is not None and
		   True):
			return synthetic_rule_70(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_71(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_expressions()) is not None and
		   True):
			return synthetic_rule_71(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_72(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('from') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return synthetic_rule_72(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_73(self):
		pos = self.mark()
		if (True and
		 (n0 := self.params()) is not None and
		   True):
			return synthetic_rule_73(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_74(self):
		pos = self.mark()
		if (True and
		 self.expect('->') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return synthetic_rule_74(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_75(self):
		pos = self.mark()
		if (True and
		 (n0 := self.func_type_comment()) is not None and
		   True):
			return synthetic_rule_75(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_76(self):
		pos = self.mark()
		if (True and
		 (n0 := self.params()) is not None and
		   True):
			return synthetic_rule_76(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_77(self):
		pos = self.mark()
		if (True and
		 self.expect('->') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return synthetic_rule_77(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_78(self):
		pos = self.mark()
		if (True and
		 (n0 := self.func_type_comment()) is not None and
		   True):
			return synthetic_rule_78(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_79(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expect('NEWLINE')) is not None and
		 (n1 := self.expect('INDENT')) is not None and
		   True):
			return synthetic_rule_79(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_80(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_etc()) is not None and
		   True):
			return synthetic_rule_80(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_81(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_etc()) is not None and
		   True):
			return synthetic_rule_81(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_82(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_etc()) is not None and
		   True):
			return synthetic_rule_82(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_83(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_etc()) is not None and
		   True):
			return synthetic_rule_83(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_84(self):
		pos = self.mark()
		if (True and
		 (n0 := self.kwds()) is not None and
		   True):
			return synthetic_rule_84(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_85(self):
		pos = self.mark()
		if (True and
		 (n0 := self.kwds()) is not None and
		   True):
			return synthetic_rule_85(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_86(self):
		pos = self.mark()
		if (True and
		 self.expect('@') is not None and
		 (n1 := self.named_expression()) is not None and
		 (n2 := self.expect('NEWLINE')) is not None and
		   True):
			return synthetic_rule_86(n1, n2)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_87(self):
		pos = self.mark()
		if (True and
		 (n0 := self.arguments()) is not None and
		   True):
			return synthetic_rule_87(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_88(self):
		pos = self.mark()
		if (True and
		 self.expect('(') is not None and
		 ((n1 := self.synthetic_rule_87()) or True) is not None and
		 self.expect(')') is not None and
		   True):
			return synthetic_rule_88(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_89(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.star_expression()) is not None and
		   True):
			return synthetic_rule_89(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_90(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_90()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_91(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.star_named_expression()) is not None and
		   True):
			return synthetic_rule_91(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_92(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_named_expression()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_91, )) is not None and
		   True):
			return synthetic_rule_92(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_93(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_93()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_94(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.expression()) is not None and
		   True):
			return synthetic_rule_94(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_95(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_95()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_96(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_params()) is not None and
		   True):
			return synthetic_rule_96(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_97(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_star_etc()) is not None and
		   True):
			return synthetic_rule_97(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_98(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_star_etc()) is not None and
		   True):
			return synthetic_rule_98(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_99(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_star_etc()) is not None and
		   True):
			return synthetic_rule_99(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_100(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_star_etc()) is not None and
		   True):
			return synthetic_rule_100(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_101(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_kwds()) is not None and
		   True):
			return synthetic_rule_101(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_102(self):
		pos = self.mark()
		if (True and
		 (n0 := self.lambda_kwds()) is not None and
		   True):
			return synthetic_rule_102(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_103(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('or') is not None and
		 (n1 := self.conjunction()) is not None and
		   True):
			return synthetic_rule_103(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_104(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('and') is not None and
		 (n1 := self.inversion()) is not None and
		   True):
			return synthetic_rule_104(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_105(self):
		pos = self.mark()
		if (True and
		 self.expect('!=') is not None and
		   True):
			return synthetic_rule_105()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_106(self):
		pos = self.mark()
		if (True and
		 (n0 := self.arguments()) is not None and
		   True):
			return synthetic_rule_106(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_107(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.slice()) is not None and
		   True):
			return synthetic_rule_107(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_108(self):
		pos = self.mark()
		if (True and
		 (n0 := self.slice()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_107, )) is not None and
		   True):
			return synthetic_rule_108(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_109(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_109()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_110(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		   True):
			return synthetic_rule_110(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_111(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		   True):
			return synthetic_rule_111(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_112(self):
		pos = self.mark()
		if (True and
		 (n0 := self.expression()) is not None and
		   True):
			return synthetic_rule_112(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_113(self):
		pos = self.mark()
		if (True and
		 self.expect(':') is not None and
		 ((n1 := self.synthetic_rule_112()) or True) is not None and
		   True):
			return synthetic_rule_113(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_114(self):
		pos = self.mark()
		if (True and
		 (n0 := self.tuple()) is not None and
		   True):
			return synthetic_rule_114(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.group()) is not None and
		   True):
			return synthetic_rule_114(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.genexp()) is not None and
		   True):
			return synthetic_rule_114(n2)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_115(self):
		pos = self.mark()
		if (True and
		 (n0 := self.list()) is not None and
		   True):
			return synthetic_rule_115(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.listcomp()) is not None and
		   True):
			return synthetic_rule_115(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_116(self):
		pos = self.mark()
		if (True and
		 (n0 := self.dict()) is not None and
		   True):
			return synthetic_rule_116(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.set()) is not None and
		   True):
			return synthetic_rule_116(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.dictcomp()) is not None and
		   True):
			return synthetic_rule_116(n2)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n3 := self.setcomp()) is not None and
		   True):
			return synthetic_rule_116(n3)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_117(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_named_expressions()) is not None and
		   True):
			return synthetic_rule_117(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_118(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_named_expressions()) is not None and
		   True):
			return synthetic_rule_118(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_119(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_named_expression()) is not None and
		 self.expect(',') is not None and
		 ((n2 := self.synthetic_rule_118()) or True) is not None and
		   True):
			return synthetic_rule_119(n0, n2)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_120(self):
		pos = self.mark()
		if (True and
		 (n0 := self.yield_expr()) is not None and
		   True):
			return synthetic_rule_120(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.named_expression()) is not None and
		   True):
			return synthetic_rule_120(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_121(self):
		pos = self.mark()
		if (True and
		 (n0 := self.assignment_expression()) is not None and
		   True):
			return synthetic_rule_121(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.expression()) is not None and
		 self.negative_lookahead(self.expect, ':=') is not None and
		   True):
			return synthetic_rule_121(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_122(self):
		pos = self.mark()
		if (True and
		 (n0 := self.double_starred_kvpairs()) is not None and
		   True):
			return synthetic_rule_122(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_123(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.double_starred_kvpair()) is not None and
		   True):
			return synthetic_rule_123(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_124(self):
		pos = self.mark()
		if (True and
		 (n0 := self.double_starred_kvpair()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_123, )) is not None and
		   True):
			return synthetic_rule_124(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_125(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_125()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_126(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('if') is not None and
		 (n1 := self.disjunction()) is not None and
		   True):
			return synthetic_rule_126(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_127(self):
		pos = self.mark()
		if (True and
		 self.expect_keyword('if') is not None and
		 (n1 := self.disjunction()) is not None and
		   True):
			return synthetic_rule_127(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_128(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_expressions()) is not None and
		   True):
			return synthetic_rule_128(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_129(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_129()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_130(self):
		pos = self.mark()
		if (True and
		 (n0 := self.assignment_expression()) is not None and
		   True):
			return synthetic_rule_130(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.expression()) is not None and
		 self.negative_lookahead(self.expect, ':=') is not None and
		   True):
			return synthetic_rule_130(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_131(self):
		pos = self.mark()
		if (True and
		 (n0 := self.assignment_expression()) is not None and
		   True):
			return synthetic_rule_131(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.expression()) is not None and
		 self.negative_lookahead(self.expect, ':=') is not None and
		   True):
			return synthetic_rule_131(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_132(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.starred_expression()) is not None and
		   True):
			return synthetic_rule_132(n1)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n2 := self.synthetic_rule_131()) is not None and
		 self.negative_lookahead(self.expect, '=') is not None and
		   True):
			return synthetic_rule_132(n2)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_133(self):
		pos = self.mark()
		if (True and
		 (n0 := self.starred_expression()) is not None and
		   True):
			return synthetic_rule_133(n0)
		self.reset(pos)
		pos = self.mark()
		if (True and
		 (n1 := self.synthetic_rule_130()) is not None and
		 self.negative_lookahead(self.expect, '=') is not None and
		 (n3 := self.loop(False, self.synthetic_rule_132, )) is not None and
		   True):
			return synthetic_rule_133(n1, n3)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_134(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.kwargs()) is not None and
		   True):
			return synthetic_rule_134(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_135(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.kwarg_or_starred()) is not None and
		   True):
			return synthetic_rule_135(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_136(self):
		pos = self.mark()
		if (True and
		 (n0 := self.kwarg_or_starred()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_135, )) is not None and
		   True):
			return synthetic_rule_136(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_137(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.kwarg_or_double_starred()) is not None and
		   True):
			return synthetic_rule_137(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_138(self):
		pos = self.mark()
		if (True and
		 (n0 := self.kwarg_or_double_starred()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_137, )) is not None and
		   True):
			return synthetic_rule_138(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_139(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.kwarg_or_starred()) is not None and
		   True):
			return synthetic_rule_139(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_140(self):
		pos = self.mark()
		if (True and
		 (n0 := self.kwarg_or_starred()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_139, )) is not None and
		   True):
			return synthetic_rule_140(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_141(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.kwarg_or_double_starred()) is not None and
		   True):
			return synthetic_rule_141(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_142(self):
		pos = self.mark()
		if (True and
		 (n0 := self.kwarg_or_double_starred()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_141, )) is not None and
		   True):
			return synthetic_rule_142(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_143(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.star_target()) is not None and
		   True):
			return synthetic_rule_143(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_144(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_144()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_145(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.star_target()) is not None and
		   True):
			return synthetic_rule_145(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_146(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_target()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_145, )) is not None and
		   True):
			return synthetic_rule_146(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_147(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_147()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_148(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.star_target()) is not None and
		   True):
			return synthetic_rule_148(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_149(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_149()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_150(self):
		pos = self.mark()
		if (True and
		 self.negative_lookahead(self.expect, '*') is not None and
		 (n1 := self.star_target()) is not None and
		   True):
			return synthetic_rule_150(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_151(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_targets_tuple_seq()) is not None and
		   True):
			return synthetic_rule_151(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_152(self):
		pos = self.mark()
		if (True and
		 (n0 := self.star_targets_list_seq()) is not None and
		   True):
			return synthetic_rule_152(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_153(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.del_target()) is not None and
		   True):
			return synthetic_rule_153(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_154(self):
		pos = self.mark()
		if (True and
		 (n0 := self.del_target()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_153, )) is not None and
		   True):
			return synthetic_rule_154(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_155(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_155()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_156(self):
		pos = self.mark()
		if (True and
		 (n0 := self.del_targets()) is not None and
		   True):
			return synthetic_rule_156(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_157(self):
		pos = self.mark()
		if (True and
		 (n0 := self.del_targets()) is not None and
		   True):
			return synthetic_rule_157(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_158(self):
		pos = self.mark()
		if (True and
		 (n0 := self.arguments()) is not None and
		   True):
			return synthetic_rule_158(n0)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_159(self):
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		 (n1 := self.double_starred_kvpair()) is not None and
		   True):
			return synthetic_rule_159(n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_160(self):
		pos = self.mark()
		if (True and
		 (n0 := self.double_starred_kvpair()) is not None and
		 (n1 := self.loop(False, self.synthetic_rule_159, )) is not None and
		   True):
			return synthetic_rule_160(n0, n1)
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_161(self):
		pos = self.mark()
		if (True and
		 self.expect('}') is not None and
		   True):
			return synthetic_rule_161()
		self.reset(pos)
		pos = self.mark()
		if (True and
		 self.expect(',') is not None and
		   True):
			return synthetic_rule_161()
		self.reset(pos)
		return None
	@memoize
	def synthetic_rule_162(self):
		pos = self.mark()
		if (True and
		 self.expect(':') is not None and
		   True):
			return synthetic_rule_162()
		self.reset(pos)
		return None

